apiVersion: apps/v1
kind: Deployment
metadata:
  name: logs-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logs-service
  template:
    metadata:
      labels:
        app: logs-service
    spec:
      containers:
        - name: logs-service
          image: squirrel-api/logs-service:v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3008
          envFrom:
            - configMapRef:
                name: squirrel-config
          env:
            - name: PORT
              value: "3008"
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: squirrel-config
                  key: REDIS_URL
            - name: SQUIRREL_INTERNAL_KEY
              valueFrom:
                secretKeyRef:
                  name: squirrel-app-secrets
                  key: SQUIRREL_INTERNAL_KEY
            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: squirrel-app-secrets
                  key: LOGS_POSTGRES_URL
            - name: S3_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: squirrel-config
                  key: S3_BUCKET
          readinessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3008
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
